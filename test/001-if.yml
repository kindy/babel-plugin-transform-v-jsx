cases:
  - i: |
      <a><b v:if={a} /></a>
    o: |
      <a>{a ? <b/> : null}</a>

  - i: |
      <a><b v:if="a" /></a>
    o: |
      <a>{"a" ? <b/> : null}</a>

  - i: |
      <a><b v:if /></a>
    o: |
      <a>{null}</a>

  - t: 'v:if in root'
    i: |
      <a v:if={a}><b /></a>
    o: |
      a ? <a><b /></a> : null

  - t: 'v:if in root'
    i: |
      <a v:if="a"><b /></a>
    o: |
      "a" ? <a><b /></a> : null

  - t: 'v:if in root'
    i: |
      <a v:if><b /></a>
    o: |
      null

  - t: 'v:if in {}'
    i: |
      <a>{<a2 v:if={1} />}<b /></a>
    o: |
      <a>{1 ? <a2 /> : null}<b /></a>

  - t: 'v:if in x={}'
    i: |
      <a x={<a2 v:if={1} />}><b /></a>
    o: |
      <a x={1 ? <a2 /> : null}><b /></a>
