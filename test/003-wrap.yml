cases:
  - i: |
      <c>
        <a v:wrap={<A v:if="1" title={'ab'} />} />
      </c>
    o: |
      <c>
        {("1" ? <A title={'ab'}><a /></A> : null)}
      </c>

  - i: |
      <c>
        <a v:if="2" v:wrap={<A/>} />
      </c>
    o: |
      <c>
        {("2" ? <A><a /></A> : null)}
      </c>

  - i: |
      <c>
        <a v:if="2" v:wrap={<A v:if="1"/>} />
      </c>
    o: |
      <c>
        {("2" ? ("1" ? <A><a /></A> : null) : null)}
      </c>

  - t: 'v:wrap in root'
    i: |
      <a v:wrap={<A v:if="1"/>} />
    o: |
      ("1" ? <A><a /></A> : null)

  - t: 'v:wrap in root'
    i: |
      <a v:if="2" v:wrap={<A />} />
    o: |
      ("2" ? <A><a /></A> : null)

  - t: 'v:wrap in root'
    i: |
      <a v:if="2" v:wrap={<A v:if="1"/>} />
    o: |
      ("2" ? ("1" ? <A><a /></A> : null) : null)
